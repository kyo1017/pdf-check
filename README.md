# Searching keywords from pdf file
## For front-end      
        npm install
        npm run start
## For back-end
        cd api
        python -m venv venv
        venv\Scripts\activate
        (venv) pip install flask flask_cors fitz PyMuPDF
        (venv) flask run